/dist/
/build/
/htmlcov/
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.eggs/
.tox/
.pytest_cache/
.coverage
*.py,cover
site.py
__pypackages__/
*.xcf

# uv
/.venv/
/uv.lock

# Local wheels cache
/whl_local/

# Packaging
/*.spec
/VERSION

# Editor
/.idea/
/.vscode/
*.swp
*~

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Claude Code personal permission log — per-machine, per-session grants.
.claude/settings.local.json

# graft's local repository settings — not committed.
/.graft/

# graft config template — may hold an API key, never commit.
.env
